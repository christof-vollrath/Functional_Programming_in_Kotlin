// Functional Sets
// ---------------

// Sets defined through functions

// Kotlin doesn't have type aliases, therefore a class is needed

class FunctionalSet { }


fun contains(functionalSet: FunctionalSet, i: Int): Boolean = false


fun singletonSet(i: Int): FunctionalSet = FunctionalSet()

