
// Given a positive integer n, final all pais of psoitive integers i and j, with i <= j < i < n such that i + j is prime.

fun pairsSumPrime(n: Int): List<Pair<Int, Int>> =
        listOf(Pair(0,0))
